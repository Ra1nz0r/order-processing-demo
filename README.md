# order-processing-demo
order-processing-demo is a small Go microservices demo that shows asynchronous order processing with gRPC, Kafka, Redis, Docker, and Kubernetes. The API service accepts orders and publishes events, while the worker consumes them and updates processing status.
